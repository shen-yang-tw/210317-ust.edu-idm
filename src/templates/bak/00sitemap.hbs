<!DOCTYPE html>
<html class="tw-text-m" lang="zh-Hant">

	<head>
		{{!-- {{>meta bootstrap=true bootstrap4=true jquery=true editor=true }} --}}
		{{>meta bootstrap=true}}
	</head>

	<body id="top" class="{{#if bodyClass}}page{{/if}}{{#if bodyFocus}} focus{{/if}}">
		{{>header hrefForAA=true logoRightNav=true mainMenu=true menuTitle1='' menuTitle2='' menuTitle3='' menuTitle4='' menuTitle5='' menuTitle6='' menuTitle6IsLink=true menuTitle6link=''}}

		{{>pageContent BreadcrumbUserVisibleOnDesk=true breadcrumbCurrent='網站導覽' userOn=true user='陳大天' linkLogout='index.html' hrefForAA=true content='sitemap' pageTitle='網站地圖'}}

		{{>footer hrefForAA=true}}

		{{#if jqueryBody}}
		<!-- jq:js -->
		<script src="js/jquery.min.js"></script>
		<!-- endinject -->
		{{/if}}
		{{#if bootstrapBody}}
		<!-- bs:js -->
		<script src="js/bootstrap.min.js"></script>
		<!-- endinject -->
		{{/if}}
	</body>

</html>